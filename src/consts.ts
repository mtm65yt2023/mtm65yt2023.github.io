export const CUSTOM_SERVER_PORT = 22044;

/**
 * Defines the minimum amount of time a client needs to wait before it can try connecting again.
 *
 * Time is in milliseconds
 */
export const JOIN_TIMEOUT = 5000;
